<UserControl x:Class="TeachingInsights2.Controls.ClientTCPInterface"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="58" Width="312"
             DataContext="{Binding TCPClientVM, Source={StaticResource Locator}}">
    <Grid Margin="10">
        <Button Command="{Binding StartConnectionCommand}" Content="Start Connection" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="130"/>
        <Button Command="{Binding StopConnectionCommand}" Content="Stop Connection" HorizontalAlignment="Left" Margin="153,10,0,0" VerticalAlignment="Top" Width="130"/>

    </Grid>
</UserControl>
