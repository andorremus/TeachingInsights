<UserControl x:Class="TeachingInsights2.Controls.ServerTCPInterface"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="60" Width="385"
             DataContext="{Binding TCPServerVM, Source={StaticResource Locator}}">
    <Grid>
        <Button Command="{Binding StartConnectionCommand}" Content="Start Connection" HorizontalAlignment="Left" Margin="52,24,0,0" VerticalAlignment="Top" Width="130"/>
        <Button Command="{Binding StopConnectionCommand}" Content="Stop Connection" HorizontalAlignment="Left" Margin="200,24,0,0" VerticalAlignment="Top" Width="130"/>

    </Grid>
</UserControl>
